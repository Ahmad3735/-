import React from 'react';

// This component is deprecated and no longer in use as part of a security enhancement.
// API key is now managed via environment variables and is not exposed to the client UI.
const ApiKeyModal: React.FC = () => {
  return null;
};

export default ApiKeyModal;
